import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.clashmeta.advisor',
  appName: 'clash-royale-meta-advisor',
  webDir: 'dist'
};

export default config;